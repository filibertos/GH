name: "Node action"
description: "Node action"

inputs:
  github-token:
    description: "github-token"
    required: true
  github-username:
    description: "github-username"
    required: true

runs:
  using: node20
  main: main.js
